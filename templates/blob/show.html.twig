{% extends 'base.html.twig' %}

{% block title %}Blob{% endblock %}

{% block body %}
    <h1>Blob</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blob.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blob.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ blob.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blob_index') }}">back to list</a>

    <a href="{{ path('app_blob_edit', {'id': blob.id}) }}">edit</a>

    {{ include('blob/_delete_form.html.twig') }}
{% endblock %}
