<form method="post" action="{{ path('app_blob_delete', {'id': blob.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blob.id) }}">
    <button class="btn">Delete</button>
</form>
